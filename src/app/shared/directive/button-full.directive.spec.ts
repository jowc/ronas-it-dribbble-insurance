import { ButtonFullDirective } from './button-full.directive';

describe('ButtonFullDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonFullDirective();
    expect(directive).toBeTruthy();
  });
});
